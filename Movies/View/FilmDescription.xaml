<Window x:Class="Movies.FilmDescription"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        DataContext="{Binding Source={StaticResource VMLocator}, Path=FilmDescriptionVM}"
        Title="LogIn" Height="600" Width="800">
    <Grid>
        <Rectangle Height="600" VerticalAlignment="Top" >
            <Rectangle.Fill>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0.5">
                    <GradientStop Color="#FF2281D1"/>
                    <GradientStop Color="#FF34268A" Offset="1"/>
                    <GradientStop Color="#FF33288B" Offset="0.5"/>
                </LinearGradientBrush>
            </Rectangle.Fill>
        </Rectangle>

        <!-- Titre du film -->
        <Label Content="Forrest Gump" HorizontalAlignment="Left" Margin="10,10,0,0" VerticalAlignment="Top" FontFamily="Segoe UI Light" FontSize="24"/>

        <!-- utiliser des étoiles -->
        <Label Content="Note 4.99/5" HorizontalAlignment="Left" Margin="700,20,0,0" VerticalAlignment="Top"/>

        <!-- Partie descriptive du film (synopsis) -->
        <TextBlock Text="Au fil des différents interlocuteurs qui viennent s'asseoir tour à tour à côté de lui sur un banc, Forrest Gump raconte la fabuleuse histoire de sa vie. Sa vie est à l'image d'une plume qui se laisse porter par le vent, tout comme Forrest se laisse porter par les événements qu'il traverse dans" Margin="50" TextWrapping="Wrap" FontFamily="Calibri" FontSize="14"/>

        <!-- Titre commentaires -->
        <Label Content="Liste des commentaires" FontSize="30" HorizontalAlignment="Left" Margin="240,150,0,0" VerticalAlignment="Top"/>

        <!-- Liste des commentaires -->
        <Label Content="Toto: Film super génial. 12/07/2019 à 17h10" HorizontalAlignment="Left" Margin="260,205,0,0" VerticalAlignment="Top"/>

        <TextBox Text="{Binding Name}" HorizontalAlignment="Left" Margin="10,520,0,0" TextWrapping="Wrap" VerticalAlignment="Top" Width="350" Foreground="White"/>

        <!-- Poster un commentaire -->
        <Button Content="Poster un commentaire" HorizontalAlignment="Left" Margin="380,520,0,0" VerticalAlignment="Top" Width="200" Click="Button_Click"/>

        <!-- Ferme cette fenêtre -->
        <Button Command="{Binding Home}" Content="Quitter" HorizontalAlignment="Right" Margin="0,520,30,0" VerticalAlignment="Top" Width="150" Click="Button_Click"/>

    </Grid>
</Window>
